[project]
name = "research-assistant-connectors"
version = "0.1.0"
description = "Allowlisted public metadata connectors for Research Assistant"
requires-python = ">=3.12"
dependencies = [
    "defusedxml==0.7.1",
    "httpx>=0.28,<1",
]

[build-system]
requires = ["uv_build>=0.10.2,<0.11.0"]
build-backend = "uv_build"
