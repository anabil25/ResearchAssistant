name: research-coordinator-smoke
agent:
    name: research-coordinator
    kind: hosted
    version: "2"
dataset:
    local_uri: evals/coordinator.jsonl
evaluators:
    - builtin.relevance
    - builtin.task_adherence
    - builtin.intent_resolution
    - builtin.indirect_attack
    - builtin.tool_call_accuracy
    - name: research-coordinator-smoke
      version: "1"
      local_uri: evaluators\research-coordinator-smoke\rubric_dimensions.json
options:
    eval_model: gpt-5.4-mini
max_samples: 15
